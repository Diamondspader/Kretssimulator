#include "Component.h"
#include <iostream>
#include <sstream>


int main(int argc, char** argv)
{
  for (int i = 0; i < argc; ++i)
  {
    if(argv[i] < 0)
    {
         //throw exception
    }
  }
  simluate();

return 0;
}
